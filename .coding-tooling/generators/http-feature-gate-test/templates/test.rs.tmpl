mod support;

use axum::http::Method;

#[tokio::test]
async fn {{name | snake}}_returns_feature_disabled() {
    let method = Method::{{method}};
    let uri = "{{path}}";
    support::assert_feature_disabled(method, uri, "{{feature}}").await;
}
