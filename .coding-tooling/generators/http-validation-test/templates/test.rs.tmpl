mod support;

use support::{ValidationCase, assert_validation_case};

#[tokio::test]
async fn {{case | snake}}_returns_bad_request() {
    assert_validation_case(ValidationCase::{{case | pascal}}).await;
}
